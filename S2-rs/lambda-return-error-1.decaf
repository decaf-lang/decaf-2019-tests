class Main {
    static void main() {
        var cmp = fun (int x) {
            if (x > 0)
                return 1;
            else if (x < 0)
                return "-1";
            else
                Print("equal");
        };

        var print = fun (int n, int[] a) {
            for (int i = 0; i < n; i = i + 1) {
                if (a[i] == 0) {
                    return i;
                };
                Print(a[i]);
            }
        };
    }
}
